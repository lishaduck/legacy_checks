name: legacy_checks
description: "Add support for legacy matchers in `package:checks` expectations."
version: 0.1.0
homepage: https://github.com/lishaduck/legacy_checks
documentation: https://github.com/lishaduck/legacy_checks
issue_tracker: https://github.com/lishaduck/legacy_checks/issues
repository: https://github.com/lishaduck/legacy_checks.git

environment:
  sdk: ^3.7.0
resolution: workspace

topics:
  - "testing"

dependencies:
  checks: ^0.3.0
  matcher: ^0.12.16+1

dev_dependencies:
  _analysis_options: ^0.0.0
  test: ^1.25.8
  very_good_cli: ^0.25.0
